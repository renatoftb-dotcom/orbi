{
  "name": "orbi",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "combine": "node combine.js",
    "cb": "node combine.js && vite build",
    "cpush": "node combine.js && vite build && git add . && git commit -m \"update\" && git push"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.1"
  }
}
